.devise-actions
  - separate = false
  - unless controller_name == 'sessions'
    = separate = separate_links(separate)
    = link_to t('devise.sessions.sign_in'), new_session_path(resource_name)
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = separate = separate_links(separate)
    = link_to t('devise.registrations.sign_up'), new_registration_path(resource_name)
  - if devise_mapping.recoverable? && controller_name != 'passwords'
    = separate = separate_links(separate)
    = link_to t('devise.passwords.forgot'), new_password_path(resource_name)
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = separate = separate_links(separate)
    = link_to t('devise.confirmations.not_receive'), new_confirmation_path(resource_name)
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = separate = separate_links(separate)
    = link_to t(''), new_unlock_path(resource_name)
  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = separate = separate_links(separate)
      = link_to "Sign in with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
